# This file is inert in the monorepo — GitHub only reads .github/ at a repository
# root — and active in the read-only split mirror, which is the point of it.
blank_issues_enabled: false
contact_links:
  - name: This repository is a read-only mirror
    url: https://github.com/sadiqk2/nativephp-symfony/issues
    about: native-symfony/desktop-bundle is published from the nativephp-symfony monorepo by subtree split. Issues and pull requests belong there; anything opened here cannot be merged.
  - name: Troubleshooting — read this first
    url: https://github.com/sadiqk2/nativephp-symfony/blob/main/docs/troubleshooting.md
    about: Symptom → cause → fix. This runtime usually fails silently rather than with an error.
